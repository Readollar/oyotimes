---
const newsItems = [
  { title: "Oyo Embraces Digital Payment Systems", link: "#" },
  { title: "New Innovation Hub Launches in Oyo Town", link: "#" },
  { title: "Smart Farming Gains Ground", link: "#" },
];
---

<section class="breaking-news">
  <div class="container">
    <span class="label">Breaking</span>
    <ul class="news-ticker">
      {newsItems.map((item) => (
        <li><a href={item.link}>{item.title}</a></li>
      ))}
    </ul>
  </div>
</section>

<style>
  .breaking-news {
    background: var(--color-accent);
    color: var(--color-text-inverse);
    padding: var(--space-8) 0;
    font-size: var(--font-body-sm-size);
    overflow: hidden;
    white-space: nowrap;
  }

  .breaking-news .container {
    display: flex;
    align-items: center;
    gap: var(--space-12);
  }

  .label {
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    flex-shrink: 0;
  }

  .news-ticker {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    animation: scroll-left 25s linear infinite;
  }

  .news-ticker li {
    margin-right: var(--space-24);
    flex-shrink: 0;
  }

  .news-ticker a {
    color: var(--color-text-inverse);
    text-decoration: none;
  }

  .news-ticker a:hover {
    text-decoration: underline;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(100%);
    }
    100% {
      transform: translateX(-100%);
    }
  }
</style>
